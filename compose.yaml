services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: court_booking
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: